tupla = (1,2,3,4,5)

print(tupla)

# No podemos modificar valores, ejemplo, usar el metodo append, cambiar valores por indice, etc..

# Si podemos iterar la tupla

for t in tupla:
    print(t)
# Crear una tupla para representar coordenadas

coordenadas = (3,5)

print(f"Coordenada x: {coordenadas[0]}")
print(f"Coordenada y: {coordenadas[1]}")